{
	"name": "search-german-words",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "npm run build && touch build/.nojekyll && gh-pages -d build -t true"
	},
	"devDependencies": {
		"@sveltejs/kit": "^1.0.0",
		"gh-pages": "^4.0.0",
		"svelte": "^3.54.0",
		"vite": "^4.0.0"
	},
	"type": "module",
	"dependencies": {
		"@picocss/pico": "^1.5.6",
		"@sveltejs/adapter-static": "^1.0.4"
	}
}
